# Local synthetic demo only. These credentials are not for public deployment.
POSTGRES_PASSWORD=counterparty-local-demo
API_PORT=8000
FRONTEND_PORT=3000
# When changing the frontend port, set a JSON array of allowed browser origins.
# ALLOWED_ORIGINS='["http://localhost:3000","http://127.0.0.1:3000"]'
DEMO_MODE=true
MODEL_MODE=demo
# For real-model evaluation only: select a model after reviewing its free quota/data terms.
GEMINI_MODEL=
GEMINI_API_KEY=
